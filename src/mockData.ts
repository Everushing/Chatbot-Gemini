// src/mockData.ts

import { UserProfile } from './types';

export const mockUserProfile: UserProfile = {
  id: '1',
  name: 'John Doe',
  preferences: {
    theme: 'light',
    language: 'en',
  },
};
